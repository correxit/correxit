name: Release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Publishing is disabled
    runs-on: ubuntu-slim
    steps:
      - run: echo "Configure the Correxit organization and package registries before enabling releases."
