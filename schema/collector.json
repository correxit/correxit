{
  "title": "Correxit Collector",
  "description": "Settings for the Correxit grade collector",
  "jupyter.lab.transform": true,
  "type": "object",
  "properties": {
    "provider": {
      "title": "Collector provider",
      "description": "Which collector to use for grade collection",
      "type": "string",
      "enum": ["manual", "moodle"],
      "default": "manual"
    },
    "moodle": {
      "title": "Moodle",
      "description": "Moodle collector settings",
      "type": "object",
      "properties": {
        "url": {
          "title": "URL",
          "description": "Base URL of the Moodle instance",
          "type": "string",
          "default": ""
        },
        "token": {
          "title": "API token",
          "description": "Moodle REST token. Stored in the secrets manager.",
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false,
      "default": {}
    }
  },
  "additionalProperties": false
}
